version: "3.8"

services:
  dashboard:
    build: .
    container_name: andina-dashboard
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
